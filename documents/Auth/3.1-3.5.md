# Sprint 3 — UI admin tipo Strapi ✅

## Entregue

| Passo | O quê |
|-------|--------|
| 3.1 | `/roles` — lista com nome, código, descrição, nº users, editar/apagar |
| 3.2 | `/roles/:id` e `/roles/new` — accordion por módulo, select all, save POST+PUT; ADMIN read-only |
| 3.3 | Form de users: select `roleId` via `GET /api/roles`; list/detail mostram `roleNome` |
| 3.4 | `*hasPermission` nos botões Novo (clientes, fornecedores, etc.) |
| 3.5 | Sidebar filtrada por módulo; Users/Roles/Relatórios só ADMIN; `adminGuard` nas rotas |

## Códigos de permissão (UI)

Os códigos seguem o sync do backend: `{segmento}.{metodoJava}` — ex. `clientes.criar`, `lancamentos.criar`.

## Checkpoint

1. Login ADMIN → `/roles` → criar “Operador lançamentos” com matriz parcial.
2. Atribuir a role a um user.
3. Login operador: sem Users/Roles/Relatórios; sem “Novo cliente” se `clientes.criar` não marcado; API 403 no create.
