# Sprint 2 — Roles dinâmicas (backend) ✅

Implementado em `modules/roles/` + migration `V5__roles_dinamicas.sql`.

## Entregue

| Passo | O quê |
|-------|--------|
| 2.1 | `V5`: tabelas `role`, `permissao`, `role_permissao`; seed ADMIN/USER; `usuario.perfil` → `role_id` |
| 2.2 | Entidades `Role`/`Permissao`; `Usuario.role`; JWT claim `role` = `role.codigo`; DTOs users com `roleId` |
| 2.3 | `PermissionCatalogSync` upsert no startup + seed leitura na role USER |
| 2.4 | `PermissionAuthorizationFilter` + Caffeine (`PermissionCacheService`); ADMIN (`sistema`) bypass |
| 2.5 | `GET/POST/PUT/DELETE /api/roles`, `GET /api/permissoes`; cache invalidada no PUT |

## Regras

- `/api/roles/**` e `/api/permissoes/**` fora do catálogo → só ADMIN (bypass).
- Autenticado sem checkbox: `/api/auth/me`, `change-password`, `logout`.
- `/auth/me`: ADMIN → `permissoes: []`; restantes → códigos da role (cache).

## Verificar após restart

1. Flyway aplica `V5`.
2. Log: `PermissionCatalogSync: N permissões sincronizadas`.
3. Login ADMIN → `GET /api/roles`, `GET /api/permissoes`.
4. Role sem `clientes.create` → `POST /api/clientes` → 403.

## Próximo

Sprint 3 — UI admin (lista/detalhe roles, select role nos users, `*hasPermission`, sidebar).
