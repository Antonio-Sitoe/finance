# Passos 1.7–1.8 — feitos (Angular)

Frontend do Sprint 1 ligado ao backend. Guia activo: [24](../24%20-%20Auth%20CRUD%20-%20Guia%20de%20implementação.md) no **1.9** (forgot/reset no backend).

Anteriores: [1.1-1.2](1.1-1.2.md) · [1.3-1.4](1.3-1.4.md) · [1.5-1.6](1.5-1.6.md)

## 1.7 — Core (`frontend/src/app/core/auth/`)

| Ficheiro | Função |
| -------- | ------ |
| `auth.service.ts` | login, loadMe, refresh, logout, forgot/reset; tokens em `sessionStorage` |
| `auth.interceptor.ts` | Bearer + retry com refresh em 401; registado em `app.config.ts` |
| `auth.guard.ts` | layout protegido; se só há refresh → refresh + `loadMe` |
| `auth.models.ts` / `auth.endpoints.ts` | DTOs e URLs alinhados com a API |

`app.routes.ts`: `canActivate: [authGuard]` no `AppLayoutComponent`.

## 1.8 — Ecrãs

| UI | Comportamento |
| -- | ------------- |
| `signin-form` | `auth.login` → `/dashboard`; erro “Email ou senha inválidos” |
| `forgot-password-form` | chama API; ecrã de sucesso sem revelar se o email existe |
| `reset-password-form` | lê `?token=`; chama `resetPassword` |
| `user-dropdown` | nome/email do `currentUser`; logout → `/signin` |

**Login de teste:** `admin@finance.com` / `Admin@123`

## Nota

O frontend **já chama** forgot/reset; o backend ainda tem stubs → reset falha até o **1.9**.

## Próximo

→ **1.9** — `PasswordResetService` + mail/log no backend
